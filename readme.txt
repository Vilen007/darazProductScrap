To install the requirements, follow the instructions below.
For Windows: type the following command in cmd, "py -m pip install requests bs4"
For Linux: type the following command in terminal, "python3 -m pip install requests bs4"

The code file is in Python3.
For Windows: type the following command in cmd, "py darazProduct.py"
For Linux: type the following command in terminal, "python3 darazProduct.py"